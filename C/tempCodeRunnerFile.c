
    // int *arr = (int*) malloc(10*sizeof(int));

    // for (int i = 0; i < 10; i++)
    // {
    //     arr[i] = i * 10;
    // }

    // for(int k = 0; k < 10; k++) printf("El numero es: %d", *arr++);